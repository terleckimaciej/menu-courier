services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: menu_courier
      POSTGRES_PASSWORD: menu_courier
      POSTGRES_DB: menu_courier
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
